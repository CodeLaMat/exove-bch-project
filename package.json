{
  "scripts": {
    "install:All": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "startfrontend": "cd frontend && npm start",
    "startbackend": "cd backend && npm start",
    "backendDev": "cd backend && npm run dev",
    "startall": "concurrently \"npm run startfrontend\" \"npm run startbackend\"",
    "buildall": "concurrently \"cd backend && npm run buildUI\" \"cd backend && npm run tsc\""
  },
  "dependencies": {
    "@types/react": "^18.0.38",
    "axios": "^1.3.4",
    "concurrently": "^7.6.0",
    "jspdf": "^2.5.1",
    "react": "^18.2.0"
  }
}
